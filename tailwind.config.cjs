const config = {
  content: ['./src/**/*.{html,js,svelte,ts}'],

  theme: {
    extend: {
      colors: {
        BackGroundMainPage: 'rgb(15 23 42/var(--tw-bg-opacity))',
      },
    },
  },

  plugins: [],
};

module.exports = config;
